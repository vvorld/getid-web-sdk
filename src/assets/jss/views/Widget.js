const widgetStyles = (theme) => ({
  root: {
    padding: '20px 0',
    fontFamily: theme.typography.fontFamily,
  },
  item: {
    textAlign: 'center',
    position: 'relative',
  },
  component: {
    marginBottom: '20px',
  },
});

export default widgetStyles;
